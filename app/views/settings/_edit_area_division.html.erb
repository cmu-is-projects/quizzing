    <div id = "edit_area_division" class="card blue-grey lighten-5">
        <div class="card-content black-text">
            <span class="card-title black-text">Edit Area Divisions</span>
            <p>By editing the settings below, you can choose which divisions are active in your area as well as setting the grade range for each. </p>
        </div>
        <div class="card-action" id="edit_card">
          <table>
            <thead>
              <tr>
                <th>Name</th>
                <th>Start Grade</th>
                <th>End Grade</th>
                <th>Active</th>
              </tr>

            </thead>
            <tbody>
              <% @divisions.each do |division| %>
                <tr>
                  <td><%= division.name.humanize %></td>
                  <td><%= division.start_grade %></td>
                  <td><%= division.end_grade %></td>
                  <% if division.active == true %>
                  <td><%= link_to toggle_division_path(division, active: false), remote: true, method: 'patch' do %>
                    <i class="mdi-toggle-check-box tiny"></i>
                    <% end %>
                  </td>
                  <% elsif division.active == false %>
                  <td><%= link_to toggle_division_path(division, active: true), remote: true, method: 'patch' do %>
                    <i class="mdi-navigation-cancel tiny"></i>
                    <% end %>
                  </td>
                  <% end %>
                </tr>
                <% end %>
            </tbody>
          </table>

        </div>

    </div>