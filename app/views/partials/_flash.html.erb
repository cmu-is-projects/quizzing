<% flash.each do |name, msg| %>
<%= javascript_tag "Materialize.toast( '#{msg}', 4000, '#{name == :alert ? 'red-text' : name == :error ? 'orange-text' : name == :notice ? 'green-text' : 'white-text' }' );" %>
<% end %>